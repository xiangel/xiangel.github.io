---
import type { HTMLAttributes } from "astro/types";
import PageViews from "./PageViews.astro";
import Socials from "./Socials.astro";
import { useTranslations } from "@/i18n";

type Props = {
  noMarginTop?: boolean;
} & HTMLAttributes<"footer">;

const { noMarginTop = false, class: className, ...attrs } = Astro.props;

const t = useTranslations(Astro.currentLocale);
const currentYear = new Date().getFullYear();
---

<footer
  class:list={["app-layout", { "mt-auto": !noMarginTop }, className]}
  {...attrs}
>
  <div
    class="border-border flex flex-col items-center justify-between gap-4 border-t py-6 sm:flex-row-reverse sm:py-4"
  >
    <Socials />
    <div class="flex flex-col items-center gap-2">
      <div class="flex flex-col items-center whitespace-nowrap sm:flex-row">
        <span>{t.footer.copyright} &#169; {currentYear}</span>
        <span class="hidden sm:inline">&nbsp;|&nbsp;</span>
        <span>{t.footer.allRightsReserved}</span>
      </div>
      <PageViews />
    </div>
  </div>
</footer>
